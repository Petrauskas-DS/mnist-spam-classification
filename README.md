# mnist-spam-classification
Comparative evaluation of machine learning models on image (Fashion-MNIST) and text (SMS spam) classification tasks using cross-validation, feature engineering, and vectorization.
